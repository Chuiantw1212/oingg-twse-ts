{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // For tsx: allow importing .ts files with extension.
    "allowImportingTsExtensions": true,
    // tsx handles transpilation, so we don't need tsc to emit files in dev.
    "noEmit": true
  }
}