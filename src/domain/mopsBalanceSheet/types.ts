/**
 * Represents the data structure for the MOPS Balance Sheet API (t164sb03).
 * The actual structure is currently unknown and needs to be determined
 * by inspecting the API response.
 */
export interface MopsBalanceSheetRecord {
  [key: string]: any;
}