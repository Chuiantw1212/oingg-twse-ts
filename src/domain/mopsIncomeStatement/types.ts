/**
 * Represents the data structure for the MOPS Cash Flow Statement API (t164sb05).
 * The actual structure is currently unknown and needs to be determined
 * by inspecting the API response.
 */
export interface MopsCashFlowStatementRecord {
  [key: string]: any;
}